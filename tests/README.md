# Tests Directory

Test suites for unit, integration, and end-to-end verification.

## Structure
- `unit/` — Unit tests for ML feature processing, graph functions, ETA math, and utility functions.
- `integration/` — Integration tests for FastAPI endpoints, database repositories, WebSocket feeds, and simulation clock ticks.
